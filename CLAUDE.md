# Claude Instructions

Read `AGENTS.md` first and treat it as the shared project source of truth.

Additional guidance for Claude in this repo:

- Favor direct, minimal edits over speculative rewrites.
- Check the current file contents before editing because this repo is often changed iteratively.
- When fixing UI behavior, inspect `index.html` and the relevant JS module in `js/` (views, admin-views, player, uploads, api, utils, live, admin, coaching). `script.js` is the entry point that assembles all mixins into `window.app`. Public view rendering (season, game, score reveal, team stats) is in `js/views.js`; admin renderers and match form actions are in `js/admin-views.js`; active scope state is in `js/coaching/state.js`; Coach Engagement dashboard methods are in `js/coaching/engagement.js`; the remaining coach workspace and My Feedback surfaces still live primarily in `js/coaching.js` while Phase 5.2 domain placeholders under `js/coaching/` preserve the future split path. UI/UX phases must use a frontend design skill before implementation/review, match the existing Replay design language, avoid raw/browser-default controls, and capture screenshots under `docs/screenshots/<phase-or-feature>/` before merge.
- Active Team/Season selector UI lives in the nav shell (`index.html` + `styles.css`) and `js/coaching/state.js` (wired through `script.js` after `apiMixin`). After auth, `loadMeScope()` reads `/api/me`; authenticated `loadMatches()` in `js/api.js` includes active `team_id` / `season_id` query params; scope changes use `PUT /api/me/scope`, then `clearScopedViewData()` replaces scoped coach/feedback/admin library content with loading placeholders before reloading. Preserve that stale-data clear before adding future scoped reloads.
- `js/coaching/state.js` owns the active Team/Season selector UI. It renders for eligible coach/admin users who have multiple teams or multiple seasons on a single team. Scope changes clear stale match arrays plus coach/feedback/admin placeholders before reloading so previous-scope rows cannot be reused after a failed refresh.
- Split CSS modules are loaded after `styles.css` in `index.html`; Phase 5.3 moves the self-contained Coach Engagement block to `styles/coaching-engagement.css` and adds `styles/` to `_STATIC_EXPORT_PATHS` in `server.py`. `settings.render_index_html()` must version every static stylesheet link it serves (`styles.css` and split CSS such as `styles/coaching-engagement.css`) so CDN/browser caches do not keep stale UI. Do the same static-export and render-version update for any future split CSS directory.
- The admin/uploader surface lives in a single `#admin-view` shell with sub-routes: `/admin/overview`, `/admin/matches`, `/admin/live`, `/admin/performance`, `/admin/users`, `/admin/settings`. Legacy URLs `/admin/streams` and `/admin/system` redirect to `/admin/live` and `/admin/performance` respectively (`LEGACY_SECTION_REDIRECTS` in `js/admin.js`). Routing, sidebar render, status-strip polling, and role gating are in `js/admin.js`. Existing renderers (`renderSettingsForm`, `renderLiveSettingsCard`, `refreshActiveStreams`, `renderUsersList`, `refreshAdminDiagnostics`, `renderTuningKnobsCard`, `renderPerformanceTuning`) are reused unchanged — only the DOM containers moved.
- Admin overview "Recent Activity" renders `diagnostics.recent_activity`, which comes from persisted SQLite `activity_events` (`db.log_activity_event`). Keep this as an operational feed for logical events: uploads, transcodes, HLS regeneration, match/user/settings/admin actions, and live/VOD-HLS stream session start/end/kill. Do not repurpose `video_errors` as the feed and do not log HLS segment polls, heartbeat noise, or per-range MP4 requests.
- `/admin/live` is the broadcast cockpit: ingest/key form on the left rail, live throughput sparkline + encoder load tile + active live viewers + stream blocks on the right. `startLiveConsolePolling` (5 s) drives the read rail; `refreshActiveStreams` writes to `#live-viewers-list` (live-kind only) and `#live-blocks-list`. `renderLiveConsoleHeader` updates the ON-AIR pill.
- `/admin/performance` stacks three full-width cards: Encoder & Host (tile grid via `renderPerformanceTuning`), Tuning Knobs (`renderTuningKnobsCard` into `#tuning-knobs-grid`, a multi-column grid where `hls_variant_presets` collapses behind a `<details>` toggle), and Disk & Diagnostics. Diagnostics rails are collapsed `<details>` accordions (`#diag-errors`, `#diag-uploads`, `#diag-transcodes`, `#diag-sessions`, `#diag-audit`); each row inside an open accordion uses the flat `.diag-row` template (no nested dark grey cards). Counts in each summary are driven by `renderAdminDiagnostics` / `renderPerformanceTuning` / `renderTuningKnobsCard`.
- Three button tiers: `.btn-primary / .btn-secondary / .btn-danger` (form submits, primary CTAs); `.btn-head` (every button inside an `.admin-panel-head` action row — Refresh, capture, copy, preset buttons, etc.); `.mini-action-btn` (row-level actions in tables / accordions). Don't use `.btn-secondary` for section-head actions — use `.btn-head` so the visual hierarchy stays consistent.
- Visible controls must be themed in both dark and light modes. Avoid raw browser chrome: no native multi-select boxes, unstyled range sliders, number spinners, default checkboxes/radios, file inputs, horizontal overflow bars, or unthemed scrollbars. Long values such as RTMP URLs should wrap within their cards instead of exposing horizontal scrollbars.
- VOD viewer counts surface per match in the matches library expanded row via `vodViewersForMatch(matchId)` (filters `this.activeStreams` to `kind === 'vod'`). The standalone `/admin/streams` section was removed.
- The Matches tab is a library table (`renderMatchLibraryTable()` in `js/admin-views.js`), not an inline form. Add/Edit go through a modal cloned from `<template id="match-form-template">` in `index.html`; the modal mounts via `app.formModal({ body, onSubmit, … })` defined in `js/ui.js`. Per-slot recovery (Verify, Regen HLS, Re-transcode, Force Re-transcode, Logs, Regenerate Thumbnail) lives inside the row's expanded diagnostics panel — that is the single place those actions are exposed. The Failed Slots / Active Jobs / Library Maintenance panels are no longer rendered under `/admin/system`; counts still surface as diagnostic tiles and the status strip.
- When fixing public-domain behavior, consider cache and proxy behavior before assuming application logic is broken.
- When adding or modifying API endpoints, update Pydantic models in `models.py` and add tests in `tests/`.
- Phase 7.2 team settings live at `routers/team_settings.py` and `services/team_settings.py`: coaches/team admins can read active-scope settings, but only team admins/global admins can PATCH AI governance/default controls. Coach > Settings is rendered in `js/coaching.js` inside the existing no-build, dark Replay component language and reloads after active team/season changes.
- Phase 8.1 AI drafting audit lives at `services/ai_drafting.py` and `ai_drafting_runs`: lifecycle helpers are service-only for now, team-scoped, and must persist compact audit metadata without raw prompts/provider outputs/private source text. Do not add provider calls, chat threads/messages, context building, or API endpoints when working in the 8.1 boundary.
- Phase 6.2 Postgres support is a compose/test lane only: `db.connect()` and startup migrations stay SQLite-backed, while `db.connect_postgres()` is an explicit smoke-lane helper. Phase 6.4 adds `python -m scripts.migrate_sqlite_to_postgres --sqlite /path/to/replay.db --database-url postgresql://...` as the one-shot import helper; it orders tables by FK dependencies, opens SQLite read-only, rejects missing DB paths, uses regular insert conflicts to fail on stale target rows, and commits only after row counts, tenant scope counts/distributions, FK orphan checks, and privacy canaries validate. Do not claim or implement full app runtime cutover before the later Alembic/runtime migration PRs. Focused validation: `pytest tests/test_sqlite_to_postgres_migration.py tests/test_postgres_lane.py tests/test_postgres_compose_static.py tests/test_postgres_migration_adr_static.py -q`; live Postgres smoke requires `REPLAY_DB_BACKEND=postgres`, `REPLAY_RUN_LIVE_POSTGRES_TESTS=1`, and `DATABASE_URL=postgresql://...`.
- Phase 7.1 team settings are service/schema-only: `team_settings` is created by `_migrate_v18`, and `services/team_settings.py` owns the closed `TEAM_SETTING_SCHEMAS`, validation exceptions, scoped reads/writes, activity audit event, and draft-target visibility helper. Do not add API/UI behavior before PR 7.2; future callers should use the service rather than writing raw JSON rows.
- Phase 6.3 durable jobs: `services/jobs.py` is the internal queue primitive. User-facing `/api/jobs*` routes are team-scoped for enqueue/read/cancel only; do not expose worker lease/heartbeat/complete/fail routes to browser sessions. Transcodes create `background_jobs` rows but still execute in-process through the existing semaphore/path; `lifespan` runs stuck-job recovery every 30s.
- Global-admin team/season/membership setup (Phase 1 PR 1.4) lives behind `auth.require_global_admin()` in `routers/admin_teams.py` and must stay service-driven through `services/teams.py`. The headless CLI (`python -m tools.admin teams|seasons|memberships ...`) uses the same service code path as the API; do not add raw SQL to the CLI. Regression coverage is `pytest tests/test_admin_teams.py -q`.
- Phase 3 split the first backend routes/services out of `server.py`: `routers/auth.py` owns login/logout/auth-check, `routers/admin.py` owns `/api/users*`, `services/visibility.py` owns coaching visibility/scrubbing, `services/engagement.py` owns coach engagement aggregation, `services/thumbnails.py` owns note/clip thumbnail generation helpers, and `services/activity.py` owns activity-feed wrappers. Keep new domain logic in these modules or a new focused router/service rather than growing `server.py`.
- Tenant scope resolution (Phase 2 PR 2.1) lives in `tenancy.py`. Use `resolve_scope(...)` / `auth.require_team_role()` for new scoped team gates; it resolves explicit `?team=`/`team_id`, then `users.last_team_id`, then a single membership, and returns a selection-required 409 for ambiguous multi-team users. Do not treat legacy `users.role=admin|coach` as an implicit bypass for normal team resources; global admin override must be an explicit `require_global_admin()`/`allow_global_admin_override=True` path. Assistant-coach capability limits are centralized in `tenancy.ROLE_CAPABILITIES`. Phase 3 moved coaching visibility helpers to `services/visibility.py`; pass resolved scope into those helpers for scoped endpoints, and keep filtering separate from note/goal `coach_private_note` scrubbing.
- Coaching routes live under `/api/coach/*` and require `admin` or `coach`; signed-in player/family feedback lives under `/api/my-feedback`. DB user roles can be comma-separated capability strings (`coach,uploader`), so use `_auth.require_role()` / `_auth.has_role()` and `app.hasRole()` / `app.canCoach()` instead of direct string equality. **Namespace exception**: `GET /api/coach/notes/{note_id}/thumbnail` is the one read-only `/api/coach/*` route reachable by any signed-in user. It is visibility-checked per-note via `_can_view_coach_note` (which reuses `_filter_notes_for_user`), so private-note thumbnails never leak. The companion `POST /api/coach/notes/{note_id}/thumbnail/regenerate` stays coach/admin-only. If a future phase needs thumbnails for private notes inside a visible playlist, add explicit playlist-context authorization at a new endpoint — do **not** weaken the standalone GET.
- Coaching drawings are JSON metadata on `coaching_notes`, rendered by `js/coaching.js` as a canvas overlay. Legacy `version: 1` strokes still render; `version: 2` objects power telestrator tools (`freehand`, `arrow`, `circle`, `zone`, `label`, `spotlight`, `dim`, `formation`). The `formation` type holds 3–16 anchors plus a `hull_points` polygon; per-anchor `player_id` is the forward-compat hook for future animated/tracked phases (see ROADMAP "Coaching Telestrator — Future Phases"). Do not burn drawings into video files unless a future clip-export feature explicitly calls for it.
- Coaching notes carry **Phase 1 structured fields** (PR 1a — `_migrate_v9` in `db.py`): `note_type` (one of `_VALID_NOTE_TYPES` = `positive` / `correction` / `question` / `team_concept` / `individual_goal`, default `correction`), `what_happened`, `why_it_matters`, `what_to_do_next`, `player_summary`, and `coach_private_note`. All optional with safe defaults — legacy clients keep working. **`coach_private_note` is coach/admin-only** — `_filter_notes_for_user` in `server.py` runs every viewer-visible note through `_strip_private_fields()` to scrub it to `""` before any `/api/my-feedback` response. `/api/my-feedback` ALSO embeds note objects under `playlists[].items[]`, so `my_feedback` builds an `items_source` for the viewer branch via `[_strip_private_fields(n) for n in all_notes]` before passing to `_playlists_with_items` — any future viewer-facing surface that embeds notes must follow the same pattern. `player_summary` is the player-facing text and IS visible to viewers by design. PR 1b surfaces the structured fields in the Coach Review composer (`renderCoachReviewForm()` + `setCoachReviewNoteType()` in `js/coaching.js`) and the Notes-tab Edit modal — keep the JS `NOTE_TYPES` array in sync with the backend `_VALID_NOTE_TYPES` set. PR 1c renders the player-facing layer in My Feedback (`renderFeedbackNotes` + the focused-modal `_renderFeedbackBody` + `renderPlaylistSessionRail`): tone pill + `player_summary || body` fallback + optional `<dl>` for `what_happened` / `why_it_matters` / `what_to_do_next`. The modal template's body container was promoted from `<p>` to `<div>` so the `<dl>` markup is valid. Helpers `_feedbackNoteSummary` / `_feedbackTonePillHtml` / `_feedbackStructuredHtml` are the single composition point — defense-in-depth: they never template `coach_private_note`.
- **Coach Review templates (Phase 2)** live in `js/coaching-templates.js` as a static registry: `COACH_TEMPLATES` (array of 14 starter templates grouped by soccer area), `COACH_TEMPLATE_GROUPS` (ordered group names for the `<optgroup>` selector), and `findCoachTemplate(id)`. Each template defines `id` / `label` / `group` / `category` / `note_type` / `title` / `player_summary` / `what_happened` / `why_it_matters` / `what_to_do_next` / `tags`. Templates **never** populate `coach_private_note` — that field stays empty unless the coach types into it. The Coach Review composer renders a Template selector + Apply / Clear buttons via `renderCoachReviewForm()`; `applyCoachTemplate()` writes the structured fields into the existing inputs and uses `confirmAction()` to ask before overwriting coach-typed text (Option A overwrite-protection — defaults like category=`shape` and tone=`correction` count as untouched on a fresh composer). The active-template tracker `_coachReviewActiveTemplateId` resets on save so the next moment starts from "None — start from scratch." When adding a new template, keep its `category` inside `NOTE_CATEGORIES` and its `note_type` inside `NOTE_TYPES` (which mirror the backend `_VALID_NOTE_TYPES` set).
- **Per-coaching-note thumbnails (Phase 3a, backend-only)** live at `<videos>/<match_id>/coach_thumbs/<note_id>.jpg`, resolved via `_media.coach_note_thumbnail_path(VIDEOS_DIR, match_id, note_id)`. The path is purely deterministic from `match_id` + `note_id` so no DB column or schema migration is required; the serving endpoint just calls `path.is_file()`. Generation runs as a background `_spawn_task(_spawn_coach_note_thumbnail(note))` from `POST /api/coach/notes` and from `PATCH /api/coach/notes/{id}` (only when `match_id` / `slot` / `timestamp_seconds` change), and uses `_media.generate_thumbnail_at_timestamp(src, dest, timestamp_s=...)` which is best-effort: missing source video, ffmpeg crash, or timestamp past duration all log a warning and return `False` rather than raising — the parent task's response to the coach is never blocked. `DELETE /api/coach/notes/{id}` `unlink(missing_ok=True)`s the JPEG. Serving: `GET /api/coach/notes/{id}/thumbnail` requires any signed-in user (`_auth.require_auth`) and enforces visibility via the new `_can_view_coach_note(user, note)` helper which **reuses `_filter_notes_for_user`** so the visibility ladder cannot drift; unknown notes, notes the user can't see, and missing files all return the same `404` so a viewer cannot probe private-note existence. Manual coach refresh: `POST /api/coach/notes/{id}/thumbnail/regenerate` (gated by `_require_coach`) returns `{ok: true, generated: bool}`. The standalone thumbnail endpoint deliberately does NOT honour the playlist-grants-access boundary for private items — same policy as `/api/my-feedback/notes`. **Caching**: the GET response uses `Cache-Control: no-cache, must-revalidate` + `ETag: "{mtime}"` (mirrors `serve_thumbnail`) — never `public`, because the response is access-controlled per-viewer and a shared cache must not replay it across users. **Path containment**: every site that resolves a coach-note thumbnail path (GET, regenerate POST, background spawn, delete cleanup) runs the path through `_thumb_path_within_videos_dir(thumb)` so a corrupted DB row whose `match_id` contains `..` cannot escape `VIDEOS_DIR` — same defense-in-depth pattern as `serve_logo` / `serve_thumbnail` (commit `e11992d`). Containment-fail in GET returns the same 404 as the unknown / unauthorized / missing-file cases.
- **Per-coaching-note thumbnails (Phase 3b, UI integration)** wires the Phase 3a endpoints into every surface that renders coaching notes. **The serving endpoint requires a Bearer header**, which `<img src>` cannot send, so `js/api.js` exposes `loadCoachNoteThumbnail(noteId)` (auth-fetch → blob → `URL.createObjectURL`) and `mountCoachNoteThumbnailsIn(container)` (one-pass mount of `<img data-coach-note-thumb="<id>">` placeholders). A per-note Map cache dedupes concurrent fetches AND negative-caches 404 / 403 / network failures so a long timeline rail doesn't re-fire requests every render. Object URLs are revoked on `setLoggedOut()` so blobs from a prior session don't outlive their visibility context — and on `regenerateCoachNoteThumbnail()` so a refreshed JPEG appears immediately. The single primitive `_coachNoteThumbHtml(note, { size })` in `js/coaching.js` produces a `.coach-thumb` tile in five size variants (`list` / `card` / `chip` / `rail` / `strip`) with a CSS `::after` film-strip placeholder so the no-thumbnail case never touches the network and never flashes a broken-image icon. Surfaces wired: Coach Notes list (with a coach-only ↻ Regenerate action that calls the existing endpoint), Coach Review timeline rail (`coach-timeline-chip--with-thumb`), Coach Playlists row (stacked `.coach-thumb-strip` of first 3 notes + `+N` overflow), My Feedback notes (220 px-thumbnail-left + body-right grid, mobile-stacks at 720 px), My Feedback playlists (first-item cover thumbnail), and the playlist session rail (active-item tile). The note picker inside the playlist edit modal stays text-only — a multi-select check list with thumbnails would change its UX meaningfully and is intentionally deferred. **Privacy**: every fetch goes through the visibility-checked GET endpoint; a viewer who can't see a note simply gets a placeholder. There is no client-side role check and no client-side authorization logic.
- **Coaching clips (Phase 4a, backend only)** are first-class objects representing a saved `[start_seconds, end_seconds]` window of a match slot. Schema lives in `coaching_clips` + `coaching_clip_players` (`db.py` `_migrate_v10`); fields mirror `coaching_notes` so the visibility ladder + linked-player rules apply unchanged. Endpoints under `/api/coach/clips` (`GET`, `GET/{id}`, `POST`, `PATCH/{id}`, `DELETE/{id}`) are coach/admin-only via `_require_coach`; clips are also embedded under `/api/my-feedback` filtered by `_filter_clips_for_user` (same shape as `_filter_notes_for_user`). MVP duration cap is `_MAX_CLIP_DURATION_SECONDS = 120.0` in `models.py`. **Privacy invariant**: `source_note_id` is OPTIONAL. When a clip is created with one, the ONLY field defaulted from the source note is the drawing snapshot (and only when the request body didn't already provide a `drawing`). All other clip fields — `match_id`, `slot`, `start_seconds`, `end_seconds`, `title`, `description`, `category`, `visibility`, `player_ids` — come from the explicit clip request and are NOT validated against the source. `body` / `coach_private_note` / `what_happened` / `why_it_matters` / `what_to_do_next` / `player_summary` / source-note title are NEVER auto-copied into the clip's response, so a coach-private note's text cannot be re-published under a more permissive clip visibility. Clip storage carries no text fields from the note — only the coach's request-body `title` + `description`. **No MP4 export yet**; playback remains seek-based against the source match video. ~~No dedicated clip thumbnails~~ — superseded by Phase 4e: clips now have first-class thumbnails generated at `start_seconds`, with source-note → co-located note → placeholder as the JS-side fallback chain. `delete_coaching_note` explicitly NULLs the `source_note_id` FK on referencing clips because SQLite's `ON DELETE SET NULL` requires `PRAGMA foreign_keys = ON` which the rest of the codebase doesn't enable; the snapshot drawing on the clip stays valid after the source note is deleted.
- **Tactical board (Phase 6c)** is a soccer-pitch sketch attached to observation notes via the existing `coaching_notes.tactical_board_json` TEXT column. **No new endpoints, no schema migration.** The board is a structured scene (NOT a raster image) with shape `{ version: 1, pitch_kind: "soccer_full", orientation: "landscape", tokens: [...], shapes: [...] }`. Tokens are `{kind: "player"|"ball", x, y, label?, player_id?}`; shapes are `{kind: "arrow"|"line"|"zone"|"label", ...}`. **Coordinates are normalized 0..1** across pitch length (x) and width (y); conversion to SVG happens at render time so a future re-render at a different size does not break saved sketches. **Validation** lives in `models.py` `validate_tactical_board_payload` — caps 40 tokens, 40 shapes, 80-char labels, 100 KB serialized blob (raw input, before per-item validation, so a corrupted client cannot force the validator to walk a multi-megabyte tokens array); rejects unknown `pitch_kind` / token-kind / shape-kind / coordinates outside [0, 1] / empty label-shape / zones extending past pitch bounds; explicitly rejects boolean `version` (Python `True == 1` would otherwise sneak through), explicit `tokens: null` / `shapes: null` (omitting the key still defaults to `[]`); normalizes input (defaults `version=1` + `orientation="landscape"`, drops unknown top-level keys for forward-compat). MVP only ships `pitch_kind: "soccer_full"` but the dispatch goes through `_VALID_PITCH_KINDS` and the JS `PITCH_RENDERERS` map so a future sport (futsal, 7-a-side, basketball, hockey) extends the set + the SVG pitch renderer — no schema migration. **Frontend module** `js/tactical-board.js` exports `renderTacticalBoardSvg(board, opts)` (read-only renderer; opts.size is `preview` / `chip` / `full`), `mountTacticalBoardSection(container, {initialBoard, getBoard, setBoard})` (the inline composer section with built-in editor mode), and `normalizeBoardForRender` / `boardHasContent` defensive helpers. The mixin is wired into `script.js` and exposed as `app.tacticalBoardSvg` / `app.mountTacticalBoardSection` / `app.tacticalBoardHasContent`. **The editor is mounted INLINE inside the observation modal**, not as a nested modal — `openAppModal` only allows one active modal at a time. The same constraint also forbids `confirmAction()` / `notifyModal()` / `promptChoice()` / `window.prompt()` from inside the section: each of those would close the parent observation `formModal` (or replace its visual context) and lose every coach-typed field. Instead the section uses a **local in-section confirm bar** (`runLocalConfirm` in `js/tactical-board.js`, host element `.tb-confirm-bar`) for Remove board / Clear board, and a **toolbar input** (`data-tb-input="label-text"`) for the Label tool's text. **Modal sizing**: the observation composer uses the new scoped `size: 'wide-board'` (1080 px max-width via `.app-modal-card.is-wide-board`) so the existing `size: 'wide'` (720 px max-width — Player Development modal, focused My Feedback player) is unaffected. **Privacy invariant**: `tactical_board_json` follows the parent note's visibility via the existing `_filter_notes_for_user` chain (board ships with the visible note payload). There is **no separate public board endpoint** and no client-side authorization logic. **Read-only viewer surfaces** (`renderCoachNotes` / `renderFeedbackNotes` / `_renderDevNoteItem`) swap the clipboard-glyph thumbnail tile for a compact SVG board preview when `tactical_board_json` has content, and add a `⌬ Board` indicator pill next to the existing `Observation` context pill. My Feedback note cards additionally render a full read-only board preview inline below the summary. Defensive rendering: an unknown `pitch_kind` or malformed payload falls through to a "No tactical board" placeholder via `normalizeBoardForRender` instead of throwing.
- **Observation notes — Coach UI (Phase 6b, frontend)** wires the Phase 6a backend into Coach. Two entry points open the same composer: Coach > Roster's clipboard-icon "Add observation" action (preselects that player + defaults visibility to `player`) and Coach > Notes' "+ New observation" button (no preselection + defaults visibility to `team`). The observation form lives in its own `<template id="coach-observation-form-template">` in `index.html` and is opened via `openCoachObservationModal(noteId, { playerId })` in `js/coaching.js`; reuses every structured field from the video-note form but swaps the match/slot/time row for `event_title` / `event_date` / `event_type` (closed enum: practice / game / meeting / tactical / other) and a "Use this for practice, sideline, tactical, or meeting feedback when no video is available." helper. **Edit dispatch**: `openCoachNoteModal` redirects to `openCoachObservationModal` when called with an observation note id, so a coach editing an observation never sees a match select that doesn't apply. **Coach > Notes list** (`renderCoachNotes`) shows a per-row context pill (`Observation` / `Video`); observation rows render event metadata (`Practice observation · Tuesday practice — scanning · 2026-05-07`) instead of `matchLabel · 0:00 · Full`, render a static clipboard-glyph thumbnail tile (no network request), and suppress the "Open in Review" + "Regenerate thumbnail" actions. **Defensive context-aware rendering** also lands in `renderFeedbackNotes` (My Feedback > Notes) and `_renderDevNoteItem` (Player Development) so a profile that mixes contexts never shows `null` / `undefined` / `NaN`; viewer-side observation cards suppress the "▶ Watch" button (no playable video) but keep "Mark reviewed". Tactical board JSON round-trips through edit (a neutral "⌬ Tactical board attached" pill surfaces when present) — the editor itself ships in Phase 6c. **Backend follow-ups in this PR (#112/#113/#114/#116)**: `_coach_note_activity_label(note)` in `server.py` falls through `title` → `event_title` → `player_summary` → `"Observation note"` for the create / update / delete activity-log messages (`coach_private_note` is intentionally excluded — the activity feed is read by every admin/uploader). `UpdateCoachingNoteRequest.title` lost its `min_length=1` so observations can PATCH `title: ""`; the route handler revalidates merged state and still rejects empty title for video notes, while observations require at least one of title / body / player_summary / what_happened / why_it_matters / what_to_do_next / event_title (or `tactical_board_json`) — same rule as create. `coach_update_note` unlinks the stale per-note JPEG (best-effort, behind `_thumb_path_within_videos_dir` containment) when a coach flips a video note to observation. `db._opt` return type widened from `str` to `Any` (used for both string fields and the JSON-encoded `tactical_board_json`). **No new endpoints, no schema changes** in 6b.
- **Unified viewer review modal (Phase 6e)** is the single review surface for every visible review type on `/feedback`. Cards on My Feedback Notes / Clips tabs and rows in the viewer-side Player Development profile are intentionally **compact** — thumb + tone pill + title + meta only. There is no inline summary, no inline tactical board, no per-card "Watch" / "Mark reviewed" / "View details" buttons. Clicking the card body opens the existing **focused feedback player modal** (`#feedback-player-template`), which is the SINGLE review surface for video notes, observation notes (text + tactical-board), and clips. The template carries the visual on top — `<video>` for notes/clips (with telestrator overlay), or a read-only tactical board (`tacticalBoardSvg(board, { size: 'preview' })`) where the video would be for observations — and the `[data-field="body"]` slot below it is filled by `_renderUnifiedFeedbackBody(target, { kind, note?, clip? })` in `js/coaching.js` which produces ONE shared structured layout: context pill + tone + category + linked players + Summary + What happened / Why / Next + Additional detail + tags. **Modal swap** (`index.html`): the template gained a `[data-field="board-wrapper"]` sibling to the video wrapper; `openFeedbackPlayer`'s `onMount` shows whichever applies for the active mode and hides the other. **Privacy invariant** (defense in depth): `_renderUnifiedFeedbackBody` NEVER references `coach_private_note` regardless of payload; the server already scrubs it via `_strip_private_fields`. `tactical_board_json` follows the parent note's visibility via the existing `_filter_notes_for_user` chain. **No new endpoints, no schema changes, no client-side authorization.** **Player Development integration**: viewer-side `_renderDevNoteItem` and `_renderDevRecentClips` route into the SAME `openFeedbackPlayer` modal via `openFeedbackNoteDetailFromDev` / `openFeedbackClipDetailFromDev` (no separate detail modal); the per-player `_feedbackDevCache` (cleared on `setLoggedOut()`) lets the click hydrate from the development payload when a recent dev row's note isn't in `_feedbackData.notes[]`. **Modal title** dispatches by kind: "Coaching Note" / observation-context label / "Coaching Clip" / "Review Session". **Mark reviewed** is the modal's confirm action for notes; clips hide the confirm button (no `clip_id` on `coaching_reviews`). **Defensive rendering**: missing structured fields collapse cleanly; observations without a board hide the board wrapper and fall through to the text layout; observation meta uses event_title / event_date / event_type; malformed boards render as the `normalizeBoardForRender` placeholder. **Card → modal contract**: when a future surface needs to show review detail, route into `openFeedbackPlayer({ mode, note? clip? })` rather than building a parallel detail-modal path — keeping the single review surface is part of the privacy story (one composer = one place where `coach_private_note` is policed).

- **Tactical board authoring tools (Phase 6d-2)** layers four improvements onto the Phase 6d-1 Coach Review tactical canvas. (1) **Game format + formation presets** live in `js/tactical-board.js` as a static `FORMATION_PRESETS` registry — `7v7` (`2-3-1` / `3-2-1` / `2-1-2-1`), `9v9` (`3-2-3` / `3-3-2` / `2-3-3` / `4-3-1`), `11v11` (`4-3-3` / `4-2-3-1` / `4-4-2` / `3-5-2` / `custom`). Public exports `formationsForGameFormat(gf)` / `formationPositions(gf, id)` are usable by future surfaces. Apply Formation **replaces existing player tokens** (after a `confirmAction` confirm when any are present) and **preserves the ball + every non-player shape** so existing arrows / zones / freehand annotations are kept. The `custom` 11v11 entry is a sentinel — applying it tags the board with `formation: "custom"` without synthesizing positions, so a hand-built board can carry the metadata. Formation positions assume left-to-right attacking direction (canonical landscape orientation). The Apply button shows a `confirmAction` dialog when player tokens already exist; ball + non-player shapes are preserved across formation changes. (2) **`tactical_board_json` metadata** — `validate_tactical_board_payload` (`models.py`) accepts two optional top-level fields: `game_format` (closed enum `_VALID_BOARD_GAME_FORMATS = {7v7, 9v9, 11v11}`) and `formation` (free-form string capped at `_MAX_BOARD_FORMATION_LENGTH = 32`, blank-rejecting). Both default to absent (NOT synthesized) so legacy 6c / 6d-1 boards round-trip unchanged. Explicit `null` is treated as absent. Unknown game_format / non-string game_format / oversized / blank formation all 422. The JS mirror (`normalizeBoardForRender` / `buildScenePayload`) only emits the fields when present. (3) **Zone resize handles** — a selected zone in editor mode renders eight resize handles (4 corners + 4 edge midpoints, each with a 22×22 px transparent hit target around an 11×11 px visible yellow square so coarse pointers grab cleanly). `beginZoneResize` clamps inside pitch bounds and to a minimum 0.02 size so a handle drag past itself can't produce a degenerate or inverted zone. Drag-to-create zone (Phase 6d-1) is unchanged. (4) **Tactical-board keyboard shortcuts** — `mountTacticalBoardReviewCanvas` binds a keydown handler that activates only when `body[data-coach-review-source="tactical_board"]`. Shortcut letters mirror the video telestrator where the tools overlap (A / F / Z / T) and use V / P / B / L for tactical-only tools (Select / Player / Ball / Line). Esc clears an armed tool first, then clears selection. The handler skips while typing in any input / textarea / select and respects modifier keys. The video shortcut handler (`_handleCoachReviewShortcut`) returns early when the active source is `tactical_board` (except for `?`) so the two layers never double-fire. (5) **Telestrator visual consistency** — the tactical tool grid uses inline-SVG icons with the SAME path strings as `renderCoachTelestratorToolbar` for every overlapping tool (Select / Arrow / Line / Zone / Pen / Label). Visible labels match too (Pen for freehand, Label for text). Tactical-only tools (Player / Ball) are first-class buttons in the same grid. The toolbar reuses the same `.coach-telestrator` + `.coach-tool-grid` + `.coach-tool-btn` + `.coach-tool-row` chain as the video telestrator (5-col icon grid on desktop, labels visible on touch, same active-state / focus ring / hover) — no tactical-only `coach-tb-tool-grid` overrides.

  **Coordinate-bug guard**: `beginDragDraw` / `beginDragFreehand` / `beginZoneResize` / `beginDrag` MUST NOT capture `stage.querySelector('svg.tb-svg')` once per drag — `refresh()` replaces the inner SVG via `innerHTML` and the captured node detaches mid-drag, so `getBoundingClientRect()` returns zeros and the shape commits at `(0, 0)`. Re-query through the `liveSvg()` helper inside every move / up handler. Captured here because the same fix needs to apply to any future drag-handler that calls `refresh()` mid-gesture. (6) **Color parity** — the tactical side panel renders the SAME six color swatches as the video telestrator (`#38bdf8` / `#f97316` / `#22c55e` / `#facc15` / `#f43f5e` / `#ffffff`) using the existing `.coach-color-swatch` class. The controller's `setActiveColor` / `getActiveColor` track the armed color; newly drawn shapes carry it via the per-shape optional `color` field on `tactical_board_json` (arrow / line / freehand / zone outline + fill / label text). Tokens stay visually distinct (no color metadata applied). Backend validation lives in `_normalize_board_color` (`models.py`) — closed `_VALID_BOARD_COLORS` set (the six palette hex strings + the legacy default `#fde047`) with `_normalize_board_color` returning None for off-palette / non-string / boolean values so they're silently dropped (NOT 422); this is defense in depth so e.g. `javascript:` URIs can't leak into `<svg fill="…">` and a corrupted client cannot DoS the note-load path. Old boards without color metadata render unchanged with the legacy default `#fde047`. (7) **Thickness parity** — the side panel also renders the SAME `<input type="range" min="2" max="10">` "W" slider as the video telestrator, beside the swatches in the same `.coach-tool-row`. Controller exposes `setActiveStrokeWidth` / `getActiveStrokeWidth`; newly drawn shapes carry it via the per-shape optional `stroke_width` integer on `tactical_board_json` (arrow / line / freehand / zone). Selection bumps visible width by 1.5 so a selected shape stays obvious at any width. Backend validation lives in `_normalize_board_stroke_width` (`models.py`, bounded `[_BOARD_STROKE_WIDTH_MIN, _BOARD_STROKE_WIDTH_MAX] = [2, 10]`) — out-of-range / non-numeric / boolean values are silently dropped (same defense-in-depth pattern as `_normalize_board_color`); float values inside the bound are rounded to int for tight column storage. Old boards without `stroke_width` metadata render with the legacy default `3`. **Privacy invariant unchanged**: `tactical_board_json` follows the parent note's visibility via `_filter_notes_for_user`; `coach_private_note` stays scrubbed for viewers; no new endpoints, no schema migration.
- **Unified Coach Review (Phase 6d-1) — review follow-ups landed in the same PR**: (a) the legacy `mountTacticalBoardSection` modal editor (used when editing existing observations) gained a `+ Pen` freehand button + drag-to-stroke handling so a coach can add a new freehand stroke without leaving the modal — same point cap (`MAX_FREEHAND_POINTS = 200`) and same shape kind as Review-mode authoring. The legacy click-then-click flow for arrow / line / zone / label / player / ball is unchanged; freehand alone uses drag because click-then-click doesn't apply. The modal's `beginDrag` (move existing items) also gained a `freehand` branch so a coach can drag a saved stroke. (b) The Phase 6d-1 source-toggle now ships full WAI-ARIA tablist keyboard navigation: ArrowLeft/Right + ArrowUp/Down move focus AND activate the next tab (automatic activation — no destructive side effects), Home/End jump to first/last, click + Enter/Space still activate. Roving tabindex (active=0, inactive=-1) is maintained by `_applyCoachReviewSource`. The selector for that tabindex update is **scoped** to `.coach-review-source-toggle [data-coach-review-source]` because `<body data-coach-review-source>` (set as the focus-drawer body-mirror) would otherwise also match. (c) The Playwright capture spec `tests/e2e/phase-6d1-capture.spec.js` is wrapped in a `test.describe.configure({ mode: 'serial' })` block so the suite runs in 1 worker by default and never trips `auth.py`'s 5/IP login rate limit, regardless of caller; the canonical invocation is `cd tests/e2e && npm run capture-phase-6d1`.
- **Unified Coach Review (Phase 6d-1)** turns Coach Review into the **single creation workspace** for video notes, video clips, and tactical-board observations. The Review shell carries `data-source="video" | "tactical_board"` and the source toggle (top of `.coach-review-picker`) swaps which children are visible via `data-source-only="…"`. Video mode shows Match / Slot / Time picker controls + Save Note + Save Clip + the existing video player + drawing canvas + telestrator side panel. Tactical Board mode swaps the picker bar to Event title / Event date / Event type + **Save Observation** (visually matched to Save Note's scale/style — `#coach-review-save-observation`), replaces the video player with the SVG pitch in the same canvas slot (`#coach-review-board-canvas`), and renders the tactical board tools + observation composer in the side panel (`#coach-tb-toolbar`, `#coach-tb-form`). **Crucially**, Tactical Board mode does NOT mount the Phase 6c `mountTacticalBoardSection` editor — that path renders its own header / Done / Cancel / inner toolbar / confirm bar inside an observation modal; reusing it inside Review would create a nested editor card with duplicate controls. A new `mountTacticalBoardReviewCanvas({stageEl, toolbarEl, statusEl, ...})` mounter in `js/tactical-board.js` mounts only the SVG canvas + an external tool grid into the side panel; the picker bar above and the Save Observation button own all save/clear actions. Both mounters share `buildBoardEditorController` for drag-to-draw arrow/line/zone, drag-to-stroke freehand, point-and-place player/ball/label, drag-to-move, and select-then-Delete. **List pages are management surfaces**: Coach > Notes "+ New note" / "+ New observation", Coach > Clips "+ New clip", and Coach > Roster's row icon button route into Coach Review with intent (`routeNewNote()` / `routeNewClip()` / `routeNewObservation({ playerId })`). The intent flows through `_coachReviewRequestedSource` + `_coachReviewIntent`, applied by `renderCoachReview()` on the next mount. Edit-existing-observation still goes through the modal `openCoachObservationModal` (which keeps using `mountTacticalBoardSection`); only **creation** moves to the Review canvas. **Tactical board schema gained `freehand`** — `_VALID_BOARD_SHAPE_KINDS` now includes `freehand`, validated as a list of 2..200 unit-coordinate points (`_MAX_BOARD_FREEHAND_POINTS`); existing boards saved before 6d-1 still load. The Phase 6c modal editor (`mountTacticalBoardSection`) is unchanged in this PR — formation presets and a 7v7/9v9/11v11 selector are deferred to 6d-2; observation rendering polish in My Feedback / Player Development is deferred to 6e. **Privacy invariant unchanged**: `tactical_board_json` follows the parent note's visibility via `_filter_notes_for_user`; `coach_private_note` stays scrubbed for viewers; the Save Observation flow uses the existing `POST /api/coach/notes` endpoint with `note_context: "observation"` — no new endpoints, no schema migration.
- **Observation notes (Phase 6a, backend only)** add support for non-video coaching notes through a single new column on `coaching_notes`: `note_context` (`video` | `observation`, default `'video'`). Migration `_migrate_v11` is purely additive — five new columns (`note_context`, `event_title`, `event_date`, `event_type`, `tactical_board_json`) plus a table-rebuild that drops `NOT NULL` from `match_id` / `slot` / `timestamp_seconds` so observation notes can omit them. Existing rows become `note_context = 'video'` (the default), preserving every prior behavior with no data migration. Re-running the migration is safe: each `ADD COLUMN` is gated by a `cols_info` check, the table-rebuild is gated on `pragma table_info(coaching_notes).<col>.notnull == 1`, and indexes use `IF NOT EXISTS`. New indexes: `idx_coaching_notes_note_context` and `idx_coaching_notes_event_date`. **Validation lives in two layers**. (1) Pydantic field-level on `CreateCoachingNoteRequest` / `UpdateCoachingNoteRequest`: `note_context` ∈ `{video, observation}` (`_VALID_NOTE_CONTEXTS`), `event_type` ∈ `{practice, game, meeting, tactical, other}` (`_VALID_EVENT_TYPES`), `event_date` matches `_DATE_RE` or empty, `tactical_board_json` is `None | dict` capped at `_MAX_TACTICAL_BOARD_JSON_BYTES = 100_000`. (2) Per-context model validator on Create + a hand-rolled merged-state check in the PATCH route handler: video notes require `match_id` + `slot` + `timestamp_seconds` + non-empty `title`; observation notes require at least one of `_OBSERVATION_CONTENT_FIELDS` (title / body / player_summary / what_happened / why_it_matters / what_to_do_next / event_title) OR a non-null `tactical_board_json` so a tactical-board-only sketch counts as meaningful content. **PATCH supports flipping `note_context`** in either direction: video → observation drops the moment-anchoring requirement (the existing match/slot/timestamp stay on the row, harmlessly), observation → video re-applies it (and revalidates the merged match_id against `_db.get_match_by_id`). PATCH `match_id: null` on a video note 422s. **Thumbnail behavior is suppressed for observation notes**: `coach_create_note` only schedules `_spawn_coach_note_thumbnail` when `note_context == 'video'`; `_spawn_coach_note_thumbnail` itself defense-in-depth bails out for observation notes; `GET /api/coach/notes/{id}/thumbnail` returns 404 immediately for observation notes (same response shape as the missing-file branch so a viewer can't probe); `POST /api/coach/notes/{id}/thumbnail/regenerate` returns `{ok: true, generated: false}` (the existing source-video-missing shape, no new branch for the frontend); `DELETE /api/coach/notes/{id}` skips the unlink attempt entirely for observation notes (no `match_id` to construct a path from). Existing video-note thumbnail flows are unchanged. **Privacy ladder is shared with video notes** — `_filter_notes_for_user` / `_strip_private_fields` work unchanged because the visibility column is the same. Observation notes flow into `/api/my-feedback` and the Phase 5a player development profile aggregation alongside video notes; `coach_private_note` is scrubbed by the existing chain on every viewer surface. `tactical_board_json` follows the parent note's visibility — no separate endpoint, no public static path. **No UI changes in 6a** — the Coach observation composer is Phase 6b, the tactical board editor is Phase 6c, unified Coach Review source modes + creation routing (creation moves to Coach Review for video notes, video clips, and tactical-board observations) are Phase 6d-1, tactical board authoring improvements + 7v7/9v9/11v11 game-format selector + per-format formation presets are Phase 6d-2, and viewer-side My Feedback / Development Profile rendering polish is Phase 6e.
- **Player development profile UI (Phase 5b)** wires the Phase 5a endpoints into Coach + My Feedback. **No backend changes.** Two API helpers in `js/api.js`: `getCoachPlayerDevelopment(playerId)` (throws on non-200) and `getMyPlayerDevelopment(playerId)` (returns `null` on 404 — backend collapses unknown / unrelated / unlinked to one code so the UI gets a single "Profile not available" path). Coach entry point is the new `chart` icon button on every Coach > Roster row → `openCoachPlayerDevelopmentModal(playerId)` mounts via `app.formModal({ size: 'wide' })`. Viewer entry point is the new `Development` sub-tab in My Feedback (`VALID_FEEDBACK_TABS` += `'development'`). When multiple linked players exist a chip selector switches profiles via `setFeedbackDevPlayer`; with one linked player the profile renders straight away. Both surfaces share `_renderPlayerDevelopmentProfile(profile, { viewer })` which paints: header → counts tile grid → themes (coach-only) → focus areas → recent positives → recent corrections → recent clips → recent playlists. Empty profile (zero counts/clips/playlists) collapses sections into a single friendly message ("No coaching activity for this player yet." / "No development feedback yet."). Note + clip thumbnails go through the existing visibility-checked `mountCoachNoteThumbnailsIn` / `mountCoachClipThumbnailsIn` so a viewer who can't see a note never sees its JPEG leak. **`coach_private_note` is intentionally never templated** in either surface — the server scrubs it for viewers; the coach surface just doesn't show it in this UI to keep the layout consistent (Phase 6 may add a coach-only block once formal goals exist). Playback integration: viewer clips → `openFeedbackClip` (existing seek-based clip player from Phase 4b), coach clips → `previewCoachClip`, viewer playlists → `openFeedbackPlaylist`. **No new playback code, no new modals.** Styling lives under `.player-dev-*` in `styles.css` with `[data-theme="light"]` overrides and a 720 px mobile collapse that drops the tile grid to two columns and stacks clip-row actions full-width.
- **Player development profiles (Phase 5a, backend)** add two aggregation endpoints over the existing coaching data — **no new tables, no schema changes, no payload changes to existing endpoints. Phase 5b adds the UI on top with no further backend changes.** `GET /api/coach/players/{id}/development` is coach/admin-only and uses the raw note/clip/playlist lists so `coach_private_note` text stays visible to coaches (mirroring `_filter_notes_for_user`'s privileged short-circuit); it adds a `linked_accounts[]` summary the viewer endpoint omits. `GET /api/my-feedback/players/{id}/development` is viewer-scoped: it requires the player to be linked to the signed-in user via `player_user_links`, and **unknown players AND unrelated viewers both return 404 (not 403)** so a viewer cannot probe whether a roster id exists. Both endpoints share a single builder `_build_player_development_profile(player, user, viewer_scoped)` so the privacy ladder cannot drift between the two surfaces. Aggregation shape: `counts` (notes/clips/playlists), `themes` (by_note_type bucket counts for the closed `_VALID_NOTE_TYPES` enum, positive_to_correction_ratio, top_categories, top_tags), `review_status` (note + playlist assigned/reviewed counts, latest_reviewed_at, reflection_count, latest_reflection — clip review tracking is **not** supported yet, surfaced as `clips.review_supported: false` rather than a misleading 0/N), `recent_notes / recent_positives / recent_corrections / recent_clips / recent_playlists` (each capped at 5, sorted newest-first), and `current_focus_areas` (derived from recent correction / individual_goal notes with non-empty `what_to_do_next`, labelled `source: "derived_from_recent_notes"` so a future client UI doesn't treat them as formal goals — Phase 6 will add explicit `player_goals` to graduate this). **Defense-in-depth privacy**: viewer notes flow through the same `_filter_notes_for_user` → `_strip_private_fields` chain `/api/my-feedback` uses, so `coach_private_note` is `""` even on team / player notes the viewer can see. Viewer reviews are scoped to `user.user_id` (so other linked accounts' reflections never leak); the coach surface returns the full assigned-review set across all users so coaches can see who has engaged with what. **Playlist association rule**: a playlist is "for" the player when either `coaching_playlist_players` includes the player (explicit tag) OR any of its ordered note items is a note tagged with the player.
- **Per-coaching-clip thumbnails (Phase 4e)** make clips first-class video objects with their own thumbnail JPEG — no longer dependent on borrowing a source-note thumbnail. Storage path is `<videos>/<match_id>/clip_thumbs/<clip_id>.jpg` resolved by `_media.clip_thumbnail_path(VIDEOS_DIR, match_id, clip_id)`. Like Phase 3a, the path is purely deterministic from `match_id` + `clip_id` so no DB column / migration ships in this PR; the serving endpoint just calls `path.is_file()`. Generation runs as a background `_spawn_task(_spawn_coach_clip_thumbnail(clip))` from `POST /api/coach/clips` and from `PATCH /api/coach/clips/{id}` when `start_seconds` changes (the clip's `match_id`/`slot` are immutable per `UpdateCoachingClipRequest`'s `extra="forbid"`); uses the existing `_media.generate_thumbnail_at_timestamp(src, dest, timestamp_s=clip.start_seconds)` helper which clamps negative timestamps to 0 and timestamps past duration to (duration - 1). Generation failures are best-effort and never block the parent clip save. `DELETE /api/coach/clips/{id}` `unlink(missing_ok=True)`s the JPEG. Serving: `GET /api/coach/clips/{id}/thumbnail` requires any signed-in user (`_auth.require_auth`) and enforces visibility via the new `_can_view_coach_clip(user, clip)` helper which **reuses `_filter_clips_for_user`** so the visibility ladder cannot drift; unknown clips, clips the user can't see, and missing files all return the same `404` so a viewer cannot probe private-clip existence. Manual coach refresh: `POST /api/coach/clips/{id}/thumbnail/regenerate` (gated by `_require_coach`) returns `{ok: true, generated: bool}`. **Caching**: `Cache-Control: no-cache, must-revalidate` + `ETag: "{mtime}"` (mirrors note thumbnails) — never `public`, because the response is access-controlled per-viewer. **Path containment**: every site (spawn, GET, regenerate POST, delete cleanup) runs the path through `_thumb_path_within_videos_dir(thumb)` so a corrupted DB row whose `match_id` contains `..` cannot escape `VIDEOS_DIR`. **Frontend**: new `loadCoachClipThumbnail(clipId)` + `mountCoachClipThumbnailsIn(container)` helpers in `js/api.js` mirror the per-note pair (auth-fetch → blob → object URL, per-clip Map cache with negative-caching, AbortController+gen for clean logout). `regenerateCoachClipThumbnail(id)` invalidates the cache so a refreshed JPEG appears on the next mount. **Rendering priority** in `_coachClipThumbHtml`: (1) clip-specific thumbnail from `/api/coach/clips/{id}/thumbnail` via `<img data-coach-clip-thumb>`, (2) source-note thumbnail if `clip.source_note_id` is set (passed as `data-coach-note-thumb-fallback` and resolved by `mountCoachClipThumbnail` after the clip GET 404s), (3) co-located note thumbnail derived from the user's bundle / feedback notes payload, (4) placeholder. Surfaces wired: Coach > Clips (`renderCoachClips`) and My Feedback > Clips (`renderFeedbackClips`). **No MP4 export**, **no clip drawing overlays** — Phase 4e scope is thumbnail only.
- **Coaching clips UI (Phase 4b)** wires Phase 4a into the Coach workspace and My Feedback. Coach gains a `Clips` sub-tab (`Roster · Notes · Playlists · Clips · Review`) listing all clips with edit/delete/preview. Coach Review picker bar has a `Save Clip` button next to `Save Note` that pre-fills `[currentTime − 5s, currentTime + 8s]` (capped at 120 s) and opens a clip composer cloned from `<template id="coach-clip-form-template">` with a live duration label that goes red on invariant violation. `source_note_id` is shown as a read-only "From note #N" pill on edit (rebinding mid-life would silently swap the captured drawing snapshot). My Feedback gains a `Clips` sub-tab; clips render as cards with a "Clip · {category}" pill + Watch button. Clip playback is seek-based: the focused feedback player gains a `mode: 'clip'` branch in `js/coaching.js` `_loadFeedbackVideoForClip` that loads the match HLS, seeks to `start_seconds` on `loadedmetadata`, and arms a dual `timeupdate`/`setInterval(250 ms)` watcher that pauses + snaps to `end_seconds` at the boundary. **No drawing overlay for clip playback** — clips don't render telestration in this phase. **No "Mark reviewed" for clips** — `coaching_reviews` accepts only `note_id` / `playlist_id` today; the Mark-reviewed CTA is suppressed in clip mode without schema changes. The frontend has zero client-side authorization; all visibility comes from the visibility-checked endpoints. **Clip-mode pointer-events fix (issue #105)**: the focused feedback player template ships a `<canvas id="feedback-drawing-canvas">` that absolute-positions over the `<video>` with `pointer-events: auto` (see `.coach-drawing-canvas` in `styles.css`) so the painter can capture strokes for note / playlist mode. Clip mode never paints, so `_loadFeedbackVideoForClip` explicitly sets the canvas `display: none` + `pointer-events: none` before arming the monitor — without that the canvas swallows every click on the native HLS chrome and the user can't press Play. Note + playlist paths still call `setupCoachCanvas` themselves and are unaffected (they end up `display: block` + `pointer-events: none` so the saved drawing renders without blocking controls). **Clip thumbnail source-note auto-derive (issue #104)**: the original Phase 4b composer never plumbed `source_note_id` through `openCoachClipModal`, so every clip created from "Save Clip" or "+ New clip" carried `source_note_id = null` and `_coachClipThumbHtml` always rendered the placeholder tile in production. Two complementary changes: (1) `openClipComposerFromReview` now calls a new `_deriveClipSourceNoteId(matchId, slot, start, end)` that prefers `_coachActiveNoteId` (the chip the coach last clicked in Coach Review) and falls back to a co-located note (same `match_id`/`slot`, `timestamp_seconds` inside `[start, end]`, closest to the window midpoint when several match), then passes it as `seed.source_note_id` so the composer renders the "From note #N" pill on CREATE and includes `source_note_id` in the POST body — keeping the explicit `extra="forbid"` PATCH guard intact for EDIT. (2) `_coachClipThumbHtml` adds a render-time fallback via `_coLocatedNoteId(clip, notes)` (shared with the composer-side derivation) so already-saved production clips with `source_note_id = null` (which can't be back-filled because `UpdateCoachingClipRequest` rejects it) still surface a thumbnail when a note in the user's bundle / `/api/my-feedback` payload sits inside the clip's window. **Privacy invariant preserved**: the fallback only picks notes already in the data the server returned for THIS user, and the thumbnail GET still runs `_can_view_coach_note` per-fetch — defense in depth, no client-side authorization. The render fallback works for both Coach > Clips (uses `_coachBundle.notes`) and My Feedback > Clips (uses `_feedbackData.notes`).
- Review playlists expose ordered `items` for playback. A visible playlist grants access to its item moments inside the playlist session even when those notes are private as standalone feedback.
- The Coach workspace at `/coach` is a single shell with four sub-tabs — **Roster · Notes · Playlists · Review** — selected via `?tab=` and routed through `setCoachTab()` in `js/coaching.js`. Notes/Playlists are list-first; their forms live in `<template id="coach-note-form-template">` / `<template id="coach-playlist-form-template">` and are mounted via `app.formModal()` (cloned so there are no duplicate IDs). The Review tab owns `<video id="coach-review-video">` + the `#coach-drawing-canvas` overlay and is the **single authoring surface** — the in-match coach side panel (`#coach-match-panel`, `#coach-mode-bar`, `#coach-mode-toggle`, `renderCoachingPanel`, `toggleCoachMode`) was removed. Coaches viewing a match get a header link `#coach-this-match-link` (set by `updateCoachThisMatchLink()` in `js/coaching.js`) that deep-links to `/coach?tab=review&match={id}&slot={current}`.
- Coach Review's Wide / Focus mode (PR #59) hides page chrome and the inspector to maximise the video + canvas. Implementation note: `openCoachFocusInspector()` in `js/coaching.js` re-parents `.coach-review-side` to `<body>` while open and restores the original DOM position on close. This is required because `.coach-tab-panel` has an `animation` property that creates a bounded stacking context — without re-parenting, the drawer's `z-index` is trapped and the backdrop renders above it (intercepting clicks). The drawer rule lives in CSS as `body.coach-focus-drawer-open > .coach-review-side` with `z-index: 110` so it clears `.main-nav` (z-index: 100). When adding new fixed-position modal-style overlays, follow the same pattern.
- Player Feedback at `/feedback` is a sub-tabbed shell — **Playlists** (default) and **Notes**, selected via `?tab=` and routed through `setFeedbackTab()`. Linked players appear as a compact chip strip (`#feedback-linked-strip`). Each note/playlist opens the **focused feedback player modal** mounted from `<template id="feedback-player-template">`. Playback never navigates to `/match/{slug}`; the modal uses `getStreamUrls()` for HLS, runs a 10 s heartbeat (`_startFeedbackHeartbeat`) so admin "kill" still propagates, and rebinds `_coachCanvasId` / `_coachVideoId` for the duration so the same telestrator painter draws on the modal canvas. The playlist controller uses the modal's video as well, so coach playlist *Preview* and player *Play* share one player.
- Match URLs use slug-based deep links (`/match/{slug}`, `/match/{slug}/first-half`). The Watch Live page deep-links to `/live`.
- Live streaming runs through a `mediamtx` sidecar in compose. Camera-facing surface is RTMP at port 1935; browsers play LL-HLS via the proxy at `/api/live/hls/*`. Never expose MediaMTX's 8888/9997 ports publicly — they stay on the internal compose network.
- When setting video status to `"error"`, always pass `error_info` dict with `error_code`, `reason`, `details` to `_set_video_status()` so errors are persisted to the `video_errors` table.
- Admin recovery endpoints live under `/api/admin/matches/{id}/...` — retry, regenerate-hls, verify, errors.
- Active streaming connections are tracked in `streams.py` and surfaced via `/api/admin/streams` (+ kill/unblock endpoints). Use `streams.client_ip(request)` whenever you need the real client IP — direct `request.client.host` reads through Cloudflare/CF Tunnel return loopback. Note: `client_ip()` only honors forwarded headers when `TRUSTED_PROXY=cloudflare` (the default); bare deployments should set `TRUSTED_PROXY=none` so only the direct peer address is used.
- Performance tuning knobs (transcode concurrency, hwaccel choice, HLS segment duration, upload limits, ABR ladder) are stored in the `settings` table — not hardcoded constants. Read live values via the `current_*()` helpers in `server.py` (e.g. `current_transcode_concurrency()`, `current_replay_hwaccel()`); each helper resolves through `settings.load_unlocked()`. Schema and validation lives in `settings.TUNING_KNOBS`. When adding a new knob: add it to `TUNING_KNOBS`, add a default to `DEFAULT_APP_SETTINGS`, add a `current_*()` helper if needed, surface it in `js/admin-views.js` via `renderTuningKnobsCard()`, and update the relevant `current_*()` consumer call sites.
- The transcode semaphore is a `ResizableSemaphore` (defined in `server.py`); `PUT /api/admin/settings` calls `TRANSCODE_SEMAPHORE.resize()` when `transcode_concurrency` changes so the change is live without a restart. Don't replace it with a plain `asyncio.Semaphore`.
- HLS cache headers must stay aligned with `live.py`'s proxy policy: playlists `public, max-age=60, must-revalidate`, segments `public, max-age=31536000, immutable`. The `Caddyfile` mirrors these — keep both in sync if the policy changes.
- Match-logo responses must emit `X-Content-Type-Options: nosniff`, `Content-Security-Policy: script-src 'none'`, and `Content-Disposition: inline` — defense-in-depth against stored XSS via uploaded SVGs (logo upload is admin/uploader-gated, but a compromised account or a careless paste of a third-party SVG should not be able to execute script in the replay app's origin). Both the FastAPI `serve_logo` route and the Caddy `@match_logo` block must keep these headers. Three places must stay in sync: `server.py` (`serve_logo`), `Caddyfile`, and the inlined Caddyfile in `docker-compose-intel.yml`.
- Storage tiering + tenant media paths: HLS variants + thumbnails go on `VIDEOS_DIR` (SSD, hot path). Raw uploads + finished MP4s go on `ORIGINALS_DIR` (cold pool when `REPLAY_ORIGINALS_DIR` is set; otherwise aliased to `VIDEOS_DIR`). New writes with a `team_id` go under `<root>/teams/<team_id>/matches/<match_id>/...`; legacy reads fall back to `<root>/<match_id>/...` via the `existing_*` media helpers. When adding a new path that holds a `<slot>.mp4` or `<slot>_raw.*` file, route it through `_slot_mp4_path()` / `_slot_raw_path()` / `_find_slot_raw_path()` in `server.py` (or the underlying `media.py` helpers) — never construct `VIDEOS_DIR / match_id / f"{slot}.mp4"` directly. HLS paths still use `_slot_hls_dir()` / `_slot_hls_master_path()` and stay on the SSD. The manual relocation helper is `scripts/relocate_to_team_paths.py --dry-run|--execute`; it is not invoked by startup/deploy.
- The Caddy reverse proxy serves VOD HLS segments directly from the `/data` bind-mount; everything else is proxied to the replay app on `:8090`. Keep both URL patterns stable during the tenant-media transition: legacy `/api/matches/{id}/hls/{slot}/...` and team-aware `/api/matches/{id}/hls/teams/{team_id}/{slot}/...`. Team-aware Caddy matchers must map only to `/videos/teams/{team_id}/matches/{id}/...` (no fallback to legacy), so wrong-team direct URLs cannot serve a legacy file before FastAPI scope checks would run. Because segments bypass FastAPI, the streams registry would otherwise lose VOD sessions to its 15 s idle reaper; `js/player.js` POSTs to `/api/matches/{id}/heartbeat?slot=…` every 10 s while playback is active to call `registry.touch("vod-hls", …)` and keep the session warm. If a heartbeat returns 403 the player stops — that's how an admin "kill" propagates to VOD viewers.
- The Performance Tuning panel polls `/api/admin/performance` every 5 s while `/admin/performance` is active. Add new signals by extending `_host_signals()` / `_disk_pools()` / the response payload — the frontend tile list in `renderPerformanceTuning()` is a flat array, easy to extend.
- Tuning knobs save through a dedicated `handleTuningSubmit()` (NOT `handleSettingsSubmit()`). It PUTs only the `collectTuningKnobs()` payload to `/api/admin/settings`, leaving branding, public-copy, and live-streaming fields untouched. The endpoint is partial-update — it iterates submitted keys only. Don't route the Performance section's `#tuning-save-btn` through `handleSettingsSubmit`; that handler reads the live-form inputs which may hold HTML defaults if the user never visited Live or Settings, silently disabling live and clearing the configured RTMP URL.
- Use the `lifespan` async context manager for startup/shutdown tasks (not `@app.on_event`).
- Deployment and troubleshooting docs live in `docs/DEPLOYMENT.md` and `docs/TROUBLESHOOTING.md`.
- Larger feature designs can live in `specs/`; the coaching MVP design note is `specs/coaching-platform-design.md`.

## Recent coaching phase implementation notes

- **Player goals (Phase 7)** are first-class action items in `player_goals`, with visibility (`player`/`coach`, default `player`), priority (`low`/`medium`/`high`, default `medium`), optional `target_date`, `success_criteria`, coach-only `coach_private_note`, status history, and viewer reflections. Coach/admin APIs can create and manage goals from player profile, note, clip, playlist item, and observation contexts. Viewer APIs expose only active `visibility=player` goals for players linked to the signed-in user AND only source evidence that passes the existing note/clip/playlist visibility filters. Viewer payloads must scrub goal/note `coach_private_note` and never include another linked account's reflection/user id; recompute `latest_reflection` from the viewer-scoped reflection list. Goal source playlists must hydrate from viewer-filtered, scrubbed note items — never raw `_db.list_coaching_notes()` — or private note text can leak through nested playlist evidence. Delete paths for players/notes/clips/playlists must explicitly delete/null goal references because SQLite FK actions are not assumed to run.
- **Match coaching summaries (Phase 8)** are match-level coach narratives with linked note/clip/playlist evidence. Summary PATCH validation must evaluate the merged existing+incoming state so an update cannot clear every text field. Viewer endpoints must return only summaries visible to the signed-in user and must hydrate evidence from viewer-filtered/scrubbed notes/clips/playlists; private source IDs/text must not appear in payloads or DOM. Summary child-reference tables need explicit cleanup on note/clip/playlist/match deletes because SQLite FK cascades are not assumed.

- **Coach engagement dashboard (Phase 9)** aggregates assigned/reviewed/reflected coaching feedback for coaches. Player filters must scope completion/reflection rows to the selected player's linked users; a review by one linked family account must not count for a different player. Private/coach-only notes are excluded from engagement metrics by default. Combined player+match playlist attribution must be computed from matching underlying note/player links, not by independently unioning playlist/player/match filters. Viewer-denied/privacy canaries belong in seed data and E2E evidence.



After every code change, update the relevant markdown files to stay in sync:

- `ROADMAP.md` — mark completed items, add descriptions of what was done.
- `AGENTS.md` — update Key Files, Editing Guidance, or Stack if new files/conventions were added.
- `CLAUDE.md` — update if validation commands or editing guidance changed.
- `specs/` — add or update a design note for non-trivial cross-stack features.

Primary validation:

```bash
python3 -m py_compile server.py && python3 -m py_compile media.py && python3 -m py_compile models.py && python3 -m py_compile db.py && python3 -m py_compile auth.py && python3 -m py_compile settings.py && python3 -m py_compile uploads.py && python3 -m py_compile log.py && python3 -m py_compile live.py && python3 -m py_compile streams.py
pytest tests/ -v --cov --cov-report=term-missing
```


- Live auth hardening: `/api/live/auth` now fails closed (503) when `LIVE_AUTH_SECRET` is unset unless `LIVE_AUTH_ALLOW_INSECURE=1` is explicitly set for dev-only use.
- Live auth secret transport: MediaMTX 1.18 dropped `authHTTPHeaders`. The shared secret now travels as the password half of HTTP Basic Auth in `authHTTPAddress` (e.g. `http://_:${MTX_LIVE_AUTH_SECRET}@replay:8090/api/live/auth`). The handler accepts both `Authorization: Basic …` (current) and `X-Internal-Secret:` (legacy) — keep both paths working when touching the handler.
- Live auth webhook tests should set `server.LIVE_AUTH_SECRET` and send the secret as either `X-Internal-Secret` or HTTP Basic Auth so stream-key validation is exercised behind the MediaMTX shared-secret gate.
- `pytest.ini` pins pytest-asyncio fixture loop scope to `function` and narrowly filters dependency-owned Python 3.14 asyncio deprecations so local test runs stay warning-clean.
- Auth token cap is configurable with `MAX_ACTIVE_TOKENS` (default 1000).
- CI gates coverage at 60% via `pytest-cov` (`.coveragerc` excludes `tests/`); current baseline is ~64%. Newer tests live in `tests/test_coaching.py` (coach roles, roster links, feedback visibility, playlist item privacy, drawing JSON validation, review tracking), `tests/test_db.py`, `tests/test_models.py`, `tests/test_server.py` (covers `ResizableSemaphore` directly), plus expanded coverage in `tests/test_admin.py`, `tests/test_matches.py`, and `tests/test_media.py`.
